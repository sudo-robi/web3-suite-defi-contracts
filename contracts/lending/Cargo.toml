[package]
name = "lending-contract"
version = "0.1.0"
edition = "2021"
description = "Decentralized lending pool with interest rate model and collateral management"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
